#include "float_vector.h"

struct float_vector {
    int capacity; // num maximo de elementos do vetor
    int size; // quant. de elementos armazenados atualmente
    float *data; // vetor de floats
};

FloatVector *create(int capacity) {
    
}